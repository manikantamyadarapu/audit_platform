/** Shared gross-weight audit workspace settings (Sales vs Purchase). */
export const SALES_GROSS_WEIGHT_AUDIT_CONFIG = {
  sessionKey: 'gross-weight',
  exportFilePrefix: 'gross-weight-rows',
  pdfTitle: 'Gross weight audit — issue register',
  successToast: 'Gross weight comparison complete',
};

export const PURCHASE_GROSS_WEIGHT_AUDIT_CONFIG = {
  sessionKey: 'purchase-gross-weight',
  exportFilePrefix: 'purchase-gross-weight-rows',
  pdfTitle: 'Purchase gross weight audit — issue register',
  successToast: 'Purchase gross weight comparison complete',
};

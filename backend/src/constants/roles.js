/**
 * Shared role sets for route authorization.
 */
const PROCESS_ROLES = ['ADMIN', 'AUDITOR', 'SUPER_ADMIN'];
const ADMIN_ROLES = ['ADMIN', 'SUPER_ADMIN'];

module.exports = {
  PROCESS_ROLES,
  ADMIN_ROLES,
};

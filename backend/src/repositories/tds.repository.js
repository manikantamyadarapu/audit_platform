/**
 * TDS rules have no local database table — rules are stored and served by
 * the Python service. This file exists as a stub to satisfy the
 * one-file-per-audit-per-layer convention.
 */
module.exports = {};

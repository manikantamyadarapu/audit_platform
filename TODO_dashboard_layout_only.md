# TODO - Dashboard layout only

- [ ] Update UI in `frontend/src/pages/SalesReturnRateAudit.jsx`:
  - [ ] Display only the 6 required summary cards (TOTAL ROWS, ERROR ROWS, ACCOUNT VS PRODUCT, RANGE DEVIATIONS, ACCESSORIES UNIT RATE CHECK, UNIT OF MEASUREMENT DEVIATIONS, COMPLIANCE) per requirements.
  - [ ] Render all summary cards in a single row on desktop (no wrapping) by using a non-wrapping flex/grid container + smaller card sizing.
  - [ ] Ensure each card shows ONLY its title/name (no value/subtitle/descriptions).
  - [ ] Remove any description/subtitle strings from the cards.
  - [ ] Keep responsive behavior: wrap on smaller screens.
- [ ] Ensure no backend logic / data / bindings / calculations are changed.
- [ ] Verify build/lint (optional) by running frontend build.

